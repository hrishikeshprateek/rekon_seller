import 'package:flutter/material.dart';

class DeliveryBookPage extends StatelessWidget {
  const DeliveryBookPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Delivery Book')),
      body: const Center(child: Text('Delivery Book page (empty)')),
    );
  }
}

